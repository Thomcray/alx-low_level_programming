## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;## Task 0x05.C - Pointers, Arrays and Strings
  0. A function that takes a pointer to and int as parameter and updates the value it points to, to 98;
