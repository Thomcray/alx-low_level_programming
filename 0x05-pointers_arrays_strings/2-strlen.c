#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string parameter
 *
 * Return: number of characters in *s
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
