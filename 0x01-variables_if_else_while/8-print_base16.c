#include <stdio.h>

/**
 * main - Prints all the numbers of 16 in lowercase followed by new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char hex[] = "0123456789abcdef";
	int k;

	for (k = 0; k <= hex[k]; k++)
	{
		putchar(hex[k]);
	}
	putchar('\n');
	return (0);
}
