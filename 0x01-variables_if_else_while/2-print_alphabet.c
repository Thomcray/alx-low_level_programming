#include <stdio.h>
/**
 * main - Prints the alphabets in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alp[] = "abcdefghijklmnopqrstuvwxyz";
	int i;

	for (i = 0; i <= alp[i]; i++)
	{
		putchar(alp[i]);
	}
	putchar('\n');
	return (0);
}
