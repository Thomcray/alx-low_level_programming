#include "main.h"

/**
 * add - Function that adds two integers and returns the result
 * @a: First parameter
 * @b: Second parameter
 *
 * Return: Returns the result of two integers
 */

int add(int a, int b)
{
	int res;

	res = (a + b);
	return (res);
}
